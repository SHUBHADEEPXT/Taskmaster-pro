apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskmaster-frontend.fullname" . }}
  labels:
    {{- include "taskmaster-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskmaster-frontend.selectorLabels" . | nindent 4 }}
